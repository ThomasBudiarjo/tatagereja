# Go
/bin/
*.test
*.out

# Runtime data (SQLite database + WAL)
/data/

# Built SPA assets (regenerated by `task fe:build`); the placeholder
# internal/frontend/dist/index.html stays tracked so the Go module compiles.
/internal/frontend/dist/assets/

# Node / frontend
/frontend/node_modules/
/frontend/.vite/
npm-debug.log*

# Editor / OS
.DS_Store
