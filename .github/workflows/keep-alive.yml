name: keep-alive
on:
  schedule:
    - cron: '*/25 6-22 * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS https://shepherd-api.herokuapp.com/health
