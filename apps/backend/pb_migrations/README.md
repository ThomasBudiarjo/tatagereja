# PocketBase migrations

PocketBase collection migrations belong in this directory. Commit generated migrations so hosted and self-hosted installations use the same schema.

The initial collections will be added with the church and membership implementation.
