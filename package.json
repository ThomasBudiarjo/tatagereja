{
  "name": "tatagereja",
  "private": true,
  "version": "0.0.0",
  "description": "Build orchestration for the TataGereja frontend (Heroku Node buildpack)",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "heroku-postbuild": "cd web && npm ci && npm run build"
  }
}
