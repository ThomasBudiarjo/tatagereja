{
  "name": "tatagereja",
  "private": true,
  "version": "0.1.0",
  "description": "Build orchestration for Heroku: the Node buildpack builds the SolidJS frontend before the Go buildpack embeds it.",
  "scripts": {
    "build": "npm --prefix web ci && npm --prefix web run build",
    "heroku-postbuild": "npm --prefix web ci && npm --prefix web run build"
  }
}
